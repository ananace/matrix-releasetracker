<%-
  avatar_str = "![avatar](#{avatar_url})" unless avatar_url.nil? || avatar_url.empty?
  publish_type = case release_type
                 when :prerelease
                   'had a pre-release created'
                 when :tag
                   'was tagged'
                 when :lightweight_tag
                   'got a lightweight tag'
                 else
                   'released'
                 end
-%>
#### [<%= avatar_str %> <%= full_name %>](<%= repo_url %>)
[<%= version_name %> <%= publish_type %> at <%= publish_date_str %>](<%= release_url %>)
<%- unless trimmed_release_notes.nil? || trimmed_release_notes.empty? %>
---
<%= trimmed_release_notes %><%= release_note_overflow %>
<%- end -%>
